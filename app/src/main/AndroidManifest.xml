<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.n0texpecterr0r.classtimetable">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".ContextApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.n0texpecterr0r.classtimetable.login.view.LoginActivity">
        </activity>
        <activity
            android:launchMode="singleTask"
            android:name="com.n0texpecterr0r.classtimetable.table.view.TableActivity">
        </activity>
        <activity android:name="com.n0texpecterr0r.classtimetable.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>